#include <iostream>
using namespace std;

void leernum (int &n);
void calculo (int n);

int main(int argc, char *argv[]) {
	int n;
	float 
	leernum (n);
	calculo (n, men);
	return 0;
}
void leernum (int n, float &men){
	int i,x,y=0;
	double men;
	men=1e30;
	do{
		cout<<"cuantos numeros quiere ingresar: ";
		cin>>x;
	} while(x<2 && x<4);
	for (i=1;i<=x;i++){
		cout<<"ingrese un numero: ";
		cin>>n;
		if (men>n)
		men=n;
	}
	//cout<<men<<" "<<endl;
}
void calculo (int n){
	cout<<men<<" "<<endl;
}
